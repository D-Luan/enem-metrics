name: Manter Backend Funcionando

on:
  schedule:
    - cron: '*/5 11-20 * * 1-5'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    
    steps:
      - name: Ping FastAPI Healthcheck
        run: curl -f ${{ vars.API_URL }}/health